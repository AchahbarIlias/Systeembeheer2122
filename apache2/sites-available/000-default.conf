#<VirtualHost *:80>
# This first listed virtual host is also the default for *:80
#        ServerName www.ilias-achahbar.sb.uclllabs.be
#        ServerAlias ilias-achahbar.sb.uclllabs.be
#        DocumentRoot /etc/www/html/defaultpage
#        <Directory /etc/www/html/defaultpage>
        #...
#        Require all granted
#        </Directory>
#</VirtualHost>

<VirtualHost *:80>
        RewriteEngine On
        RewriteCond %{HTTPS} !=on
        RewriteRule ^/?(.*) https://%{SERVER_NAME}/$1 [R=301,L]
</virtualhost>
<VirtualHost *:443>
        ServerAdmin www.ilias-achahbar.sb.uclllabs.be
        DocumentRoot /etc/www/html/defaultpage
        ErrorLog ${APACHE_LOG_DIR}/error.log
        CustomLog ${APACHE_LOG_DIR}/access.log combined
        SSLEngine on
        SSLCertificateFile /etc/apache2/certificate/apache-certificate.crt
        SSLCertificateKeyFile /etc/apache2/certificate/apache.key
</VirtualHost>
